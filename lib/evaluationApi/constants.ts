/**
 * Relative paths appended to `EVAL_BACKEND_BASE_URL` when proxying from Next route handlers.
 * Adjust to match your real API.
 */
export const BACKEND_PATH_EVALUATIONS = '/v1/evaluations';
export const BACKEND_PATH_REGIONS = '/v1/regions';
